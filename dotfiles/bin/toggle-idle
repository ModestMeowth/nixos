#!/usr/bin/env bash

if pgrep -x hypridle >/dev/null; then
    pkill -x hypridle
    notify-send "System locking when idle disabled"
else
    uwsm app -- hypridle >/dev/null 2>&1 &
    notify-send "System locking when idle enabled"
fi

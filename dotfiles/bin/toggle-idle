#!/usr/bin/env bash

if pgrep -f hypridle >/dev/null; then
    systemctl --user stop hypridle
    notify-send -u low "󱫖    System locking when idle disabled"
else
    systemctl --user start hypridle
    notify-send -u low "󱫖    System locking when idle enabled"
fi

pkill -RTMIN+9 waybar

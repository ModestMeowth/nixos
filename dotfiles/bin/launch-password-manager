#!/usr/bin/env bash

password_manager=""
for candidate in 1password bitwarden; do
    if command -v "$candidate"; then
        password_manager="$candidate"
        break
    fi
done

launch-or-focus "$password_manager" "$@"

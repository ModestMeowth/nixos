#!/usr/bin/env bash

if command -v uwsm &>/dev/null; then
    if uwsm check may-start; then
        exec setsid uwsm-app -- xdg-terminal-exec -e "$1" "${@:2}"
        exit 0
    fi
fi

exec setsid -- xdg-terminal-exec -e "$1" "${@:2}"
